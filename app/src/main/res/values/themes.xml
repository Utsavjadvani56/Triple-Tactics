<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TripleTactics" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TripleTactics" parent="Base.Theme.TripleTactics" />

    // Registration Activity Edittext Style
    <style name="Registration_txt" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:background">@drawable/textboxbg</item>
        <item name="fontFamily">@font/lexend_bold</item>
        <item name="android:paddingStart">10dp</item>
        <item name="android:textSize">18sp</item>
    </style>


</resources>